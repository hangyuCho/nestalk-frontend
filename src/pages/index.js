import React from 'react';
import styled from 'styled-components';

const MainStyle = styled.div`
  font-size: 30px;
  color: red;
`;

const Home = () => {
  return <MainStyle>Hello. next</MainStyle>;
};

export default Home;
