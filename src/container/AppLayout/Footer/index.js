import React, { memo } from 'react';

import FooterWrapper from './styled';

const Footer = memo(() => (
  <FooterWrapper>
    <div>
      <div>
        <span>kakaoProject</span>
      </div>
    </div>
  </FooterWrapper>
));

export default Footer;
